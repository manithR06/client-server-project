# client-server-project
Secure Multi-Process TCP Application Server developed in C using socket programming, fork()-based concurrency, custom client-server protocol, user authentication, session management, brute-force protection, logging, and process management on Linux.
